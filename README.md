# Decodex vers automoderator
Un merveilleux script Python pour convertir le Décodex (LeMonde) en règles AutoModerator (Reddit).

## Usage :

```sh
python3 decodex_vers_automoderator.py > regles
```
